Detect the connected Fluent version on ``connect`` and target the offline settings-schema checks at it; add ``FLUIDS_MCP_FLUENT_VERSION`` to select a vendored ``settings_<v>.json.gz`` snapshot (e.g. for attaching to Fluent 2024 R1)
