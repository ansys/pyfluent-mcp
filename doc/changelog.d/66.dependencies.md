Pin ``ansys-fluent-core`` to ``>=0.37.2,<0.38`` -- the last line that supports Fluent 2024 R1 (24.1); 0.38 raised the floor to Fluent 2024 R2. This also caps ``requires-python`` at ``<3.14`` (0.37.x requires ``<3.14``), so Python 3.14 is dropped from the classifiers and CI matrix.
